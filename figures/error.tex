\begin{figure}[ht]
  \centering
  % Use .5\textwidth to only have the table fill half the page,
  % and \textwidth for the full page
  \includegraphics[width=1\textwidth]{figures/error.png}
  \caption{NoRef trait forces a compile-time error message instead of run-time undefined behavior, if pointers 
  are captured by a delegated closure.}
  \label{fig:error}
\end{figure}
